# 🔄 后端修改说明文档

## 📋 修改概述
为了让队友了解前端需要的后端修改，以下是详细的修改内容：

## 🎵 1. DuckSound.java 修改

### ⚠️脆鼠修改：添加情绪声音支持
```java
// 原有代码：
DUCKLING_QUACK_1("小鸭子：嘎~（奶声奶气）", "/sounds/duckling.wav"),
DUCKLING_QUACK_2("小鸭子：嘎嘎嘎！（超可爱）", "/sounds/duckling.wav");

// ⚠️脆鼠修改：新增三种情绪声音
DUCKLING_HAPPY("小鸭子：嘎嘎嘎！（开心快乐）", "/sounds/duckling_happy.wav"),
DUCKLING_SAD("小鸭子：嘎...（伤心失落）", "/sounds/duckling_sad.wav"),
DUCKLING_CONFIDENT("小鸭子：嘎！嘎嘎嘎！（自信满满）", "/sounds/duckling_confident.wav");
```

### 🔧 新增方法
```java
/**
 * ⚠️脆鼠修改：获取对应情绪的声音
 * 好处：根据情绪类型快速定位对应声音
 */
public static DuckSound getEmotionSound(String emotion) {
    switch (emotion.toLowerCase()) {
        case "happy": return DUCKLING_HAPPY;
        case "sad": return DUCKLING_SAD;
        case "confident": return DUCKLING_CONFIDENT;
        default: return DUCKLING_HAPPY; // 默认返回开心声音
    }
}
```

## 🦆 2. DuckBehaviorService.java 修改

### ⚠️脆鼠修改：适配新的声音系统
```java
// 原有代码：
private static final List<DuckSound> DUCKLING_SOUNDS = List.of(
        DuckSound.DUCKLING_QUACK_1,
        DuckSound.DUCKLING_QUACK_2
);

// ⚠️脆鼠修改：小鸭子情绪声音集合
private static final List<DuckSound> DUCKLING_EMOTION_SOUNDS = List.of(
        DuckSound.DUCKLING_HAPPY,
        DuckSound.DUCKLING_SAD,
        DuckSound.DUCKLING_CONFIDENT
);
```

### 🔄 方法调用修改
```java
// 原有代码：
sound = randomPick(DUCKLING_SOUNDS);

// ⚠️脆鼠修改：使用新的情绪声音集合
sound = randomPick(DUCKLING_EMOTION_SOUNDS);
```

## 📁 3. 需要的声音文件

### 🎵 新增声音文件路径
```
src/main/resources/sounds/
├── duckling_happy.wav      # 开心嘎嘎声
├── duckling_sad.wav        # 伤心嘎嘎声
├── duckling_confident.wav   # 自信嘎嘎声
├── duckling.wav           # 原有小鸭子声音
└── donald.wav            # 唐老鸭声音
```

### 🎙️ 声音文件要求
- **格式**：WAV格式（Java Sound API原生支持）
- **时长**：1-3秒为宜
- **音质**：16bit, 44.1kHz
- **大小**：每个文件不超过1MB

## 🖼️ 4. 前端图片资源

### 📸 新增情绪图片
```
src/main/resources/images/
├── happy.png      # 开心状态的小鸭子
├── sad.png        # 伤心状态的小鸭子
├── confident.png  # 自信状态的小鸭子
├── smallduck.png # 默认小鸭子
├── largeduck.png # 唐老鸭
├── beidaiku.png   # 背带裤图片
├── hat.png        # 帽子图片
└── hudiejie.png   # 蝴蝶结图片
```

## 🔄 5. 工作流程说明

### 🎯 前端调用流程
1. **用户点击小鸭子** → `handleDuckClick()`
2. **调用后端服务** → `behaviorService.getBehavior(DUCKLING)`
3. **随机返回情绪声音** → `DUCKLING_HAPPY/SAD/CONFIDENT`
4. **前端设置情绪状态** → `duck.setEmotion(emotionType)`
5. **播放对应声音** → `playDuckSound(behavior)`
6. **切换显示图片** → `happy.png/sad.png/confident.png`

### 🔗 前后端交互
```java
// 前端调用
DuckBehaviorService.DuckBehavior behavior = behaviorService.getBehavior(DuckRole.DUCKLING);

// 后端返回随机情绪
// 可能返回：DUCKLING_HAPPY, DUCKLING_SAD, DUCKLING_CONFIDENT

// 前端处理
String emotionType = getEmotionFromSound(behavior.getSound());
duck.setEmotion(emotionType);
```

## 🎮 6. 换装系统集成

### 👕 衣柜系统
前端已实现完整的换装系统：
- **真实服装图片**：hat.png, beidaiku.png, hudiejie.png
- **优雅降级**：图片加载失败时使用手绘后备方案
- **服装分类**：帽子、背带裤、蝴蝶结等配饰

### 🎨 图片加载优先级
```java
// ⚠️脆鼠修改：优先使用真实图片，后备手绘 - 软工思想：优雅降级
if (clothingImagesLoaded && hatImage != null) {
    // 使用真实帽子图片
    g2d.drawImage(hatImage, hatX, hatY, null);
} else {
    // ⚠️脆鼠修改：后备手绘方案
    // 手绘帽子作为后备
}
```

## 🚀 7. 部署说明

### 📦 需要添加的文件
1. **声音文件**（队友需要提供）：
   - `src/main/resources/sounds/duckling_happy.wav`
   - `src/main/resources/sounds/duckling_sad.wav`
   - `src/main/resources/sounds/duckling_confident.wav`

2. **图片文件**（已存在）：
   - `src/main/resources/images/happy.png`
   - `src/main/resources/images/sad.png`
   - `src/main/resources/images/confident.png`
   - `src/main/resources/images/beidaiku.png`
   - `src/main/resources/images/hat.png`
   - `src/main/resources/images/hudiejie.png`

### ⚙️ 编译要求
```bash
# 确保项目包含所有资源文件
mvn clean compile
mvn exec:java  # 测试运行
```

## 🎯 8. 测试要点

### 🔊 声音测试
- [ ] 开心声音播放正常
- [ ] 伤心声音播放正常
- [ ] 自信声音播放正常
- [ ] 声音文件路径正确

### 🖼️ 图片测试
- [ ] 开心状态显示happy.png
- [ ] 伤心状态显示sad.png
- [ ] 自信状态显示confident.png
- [ ] 服装图片显示正常
- [ ] 图片加载失败时降级到手绘

### 🎮 交互测试
- [ ] 点击小鸭子触发声音+情绪切换+动画
- [ ] 换装界面正常显示真实服装图片
- [ ] 服装切换无冲突
- [ ] 动画效果流畅

## 📞 9. 联系方式

如果后端有任何问题或需要调整，请及时沟通：
- **前端需求**：情绪声音+图片切换+真实服装图片
- **关键文件**：DuckSound.java, DuckBehaviorService.java
- **测试重点**：声音播放、图片显示、服装系统

---

🎯 **总结**：
1. **后端修改**：添加3种情绪声音枚举，修改声音集合
2. **前端实现**：情绪图片切换、真实服装图片、优雅降级
3. **资源需求**：3个WAV声音文件，服装图片已存在
4. **功能特色**：点击触发情绪切换+声音播放+服装换装

所有代码修改都添加了"⚠️脆鼠修改"注释，便于识别和维护！
